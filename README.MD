#Spring JMS playground

Simple example usage of Spring JMS with ActiveMQ.

`spring-jms-publisher` generates Order object and sends it to queue, as JSON.
`spring-jms-consumer` grabs Order object from queue